<LoadingScreen>:
    Label:
        # text: app._('status_loading', app.locale) + ' ' + str(root.status) + '%'
        text: app._('status_loading', app.locale)
        font_size: '24sp'
        size_hint: None, None
        size: self.texture_size
        pos_hint: {'center_x': 0.5, 'center_y': 0.5}
