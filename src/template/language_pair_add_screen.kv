# Copyright 2025 The terCAD team. All rights reserved.
# Use of this source code is governed by a CC BY-NC-ND 4.0 license that can be found in the LICENSE file.

<LanguagePairAddScreen>:
    BoxLayout:
        orientation: 'vertical'
        padding: dp(15)
        spacing: dp(15)

        # Header
        BoxLayout:
            size_hint_y: None
            height: dp(56)
            Label:
                text: app._("language_pair_add_title", app.locale)
                font_size: sp(22)
                bold: True

        # Form fields
        ScrollView:
            size_hint_y: 0.8

            BoxLayout:
                orientation: 'vertical'
                spacing: dp(15)
                size_hint_y: None
                height: self.minimum_height
                padding: [dp(10), 0]

                # Source Language - REQUIRED
                BoxLayout:
                    orientation: 'vertical'
                    size_hint_y: None
                    height: dp(180)
                    spacing: dp(8)
                    Label:
                        text: app._("language_pair_from_label", app.locale)
                        size_hint_y: None
                        height: dp(36)
                        font_size: sp(16)
                        text_size: self.size
                        halign: 'left'
                        valign: 'middle'
                    # Language selection tiles (dynamic)
                    RecycleView:
                        id: from_view
                        viewclass: 'LanguageTileWidget'
                        size_hint_y: None
                        height: dp(130)
                        RecycleGridLayout:
                            cols: 4
                            default_size: None, dp(70)
                            default_size_hint: 1, None
                            size_hint_y: None
                            height: self.minimum_height
                            spacing: dp(8)

                # Target Language - REQUIRED
                BoxLayout:
                    orientation: 'vertical'
                    size_hint_y: None
                    height: dp(180)
                    spacing: dp(8)
                    Label:
                        text: app._("language_pair_to_label", app.locale)
                        size_hint_y: None
                        height: dp(36)
                        font_size: sp(16)
                        text_size: self.size
                        halign: 'left'
                        valign: 'middle'
                    # Language selection tiles (dynamic)
                    RecycleView:
                        id: to_view
                        viewclass: 'LanguageTileWidget'
                        size_hint_y: None
                        height: dp(130)
                        RecycleGridLayout:
                            cols: 4
                            default_size: None, dp(70)
                            default_size_hint: 1, None
                            size_hint_y: None
                            height: self.minimum_height
                            spacing: dp(8)

                # Name - Auto-generated, but editable
                BoxLayout:
                    orientation: 'vertical'
                    size_hint_y: None
                    height: dp(100)
                    spacing: dp(8)
                    Label:
                        text: app._("language_pair_name_label", app.locale)
                        size_hint_y: None
                        height: dp(36)
                        font_size: sp(16)
                        text_size: self.size
                        halign: 'left'
                        valign: 'middle'
                    TextInput:
                        id: name_input
                        text: root.name_text
                        multiline: False
                        size_hint_y: None
                        height: dp(48)
                        font_size: sp(18)
                        on_text: root.name_text = self.text

                # Logo Path - Optional
                BoxLayout:
                    orientation: 'vertical'
                    size_hint_y: None
                    height: dp(100)
                    spacing: dp(8)
                    Label:
                        text: app._("language_pair_logo_label", app.locale)
                        size_hint_y: None
                        height: dp(36)
                        font_size: sp(16)
                        text_size: self.size
                        halign: 'left'
                        valign: 'middle'
                    TextInput:
                        id: logo_input
                        text: root.logo_path_text
                        multiline: False
                        size_hint_y: None
                        height: dp(48)
                        font_size: sp(16)
                        hint_text: "e.g., assets/images/language/pair_logo.png"
                        on_text: root.logo_path_text = self.text

        # Action buttons
        BoxLayout:
            orientation: 'horizontal'
            size_hint_y: None
            height: dp(56)
            spacing: dp(15)
            Button:
                text: app._("button_cancel", app.locale)
                font_size: sp(18)
                on_release: root.cancel()
            Button:
                text: app._("button_save", app.locale)
                font_size: sp(18)
                on_release: root.save_language_pair()


<LanguageTileWidget@ButtonBehavior+BoxLayout>:
    locale: ''
    text: ''
    logo: ''
    selected: False
    is_from: False
    orientation: 'vertical'
    padding: dp(8)
    spacing: dp(4)

    canvas.before:
        Color:
            rgba: (0.3, 0.7, 0.3, 1) if self.selected else (0.2, 0.2, 0.2, 0.8)
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [dp(10)]

    on_release:
        # Call appropriate selection method based on is_from flag
        app.root.get_screen('language_pair_add_screen').select_from_language(self.locale) if self.is_from else app.root.get_screen('language_pair_add_screen').select_to_language(self.locale)

    Label:
        text: root.locale
        font_size: sp(18)
        bold: True
        size_hint_y: 0.4
        color: (1, 1, 1, 1)

    Label:
        text: root.text
        font_size: sp(12)
        size_hint_y: 0.6
        text_size: self.width, None
        halign: 'center'
        color: (0.9, 0.9, 0.9, 1)